@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
@model IEnumerable<Hotel_Management.Areas.Roomstatus.Models.LOC_RoomStatusModel>;

<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Staff Search</h5>
                <form method="post" asp-controller="RoomStatus" asp-action="StatusSearch" asp-area="Roomstatus" class="d-flex w-80" role="search">
                    <input class="form-control me-2 m-3 border border-info" type="text" placeholder="status" name="status" />
                    <button type="submit" class="btn btn-outline-success my-3 h-50">Filter</button>
                </form>
                <div class="table-responsive pt-3">
                    <table class="table table-striped project-orders-table">
                        <thead>
                            <tr>
                                <th class="ml-5">Status ID</th>
                                <th>Room Status Type</th>
                                <th>Created</th>
                                <th>Modified</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        @foreach (var row in Model)
                        {
                            <tbody>
                                <tr>
                                    <td>@row.StatusID</td>
                                    <td>@row.Status</td>
                                    <td>@Convert.ToDateTime(row.Created).ToString("dd-MM-yyyy")</td>
                                    <td>@Convert.ToDateTime(row.Created).ToString("dd-MM-yyyy")</td>
                                    <td>
                                        <div class="d-flex align-items-center">
                                            <form method="post" asp-area="Roomstatus" asp-controller="RoomStatus" asp-action="StatusAddEdit" asp-route-StatusID="@row.StatusID">
                                                <button type="submit" class="btn btn-success btn-sm btn-icon-text mr-3">
                                                    Edit
                                                    <i class="typcn typcn-edit btn-icon-append"></i>
                                                </button>
                                            </form>
                                            <form method="post" asp-area="Roomstatus" asp-controller="RoomStatus" asp-action="DeleteByStatusID" asp-route-StatusID="@row.StatusID">
                                                <button type="submit" class="btn btn-danger btn-sm btn-icon-text">
                                                    Delete
                                                    <i class="typcn typcn-delete-outline btn-icon-append"></i>
                                                </button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                            </tbody>
                        }
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
@* <div class="content-wrapper">
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Status Search</h5>
                        <form method="post" asp-controller="RoomStatus" asp-action="StatusSearch" asp-area="Roomstatus" class="d-flex w-80" role="search">
                            <input class="form-control me-2 m-3" type="text" placeholder="Status" name="Status" />
                            <button type="submit" class="btn btn-outline-success my-3 h-50">Filter</button>
                        </form>
                        <div class="table-responsive">
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th scope="col" class="text-center">Status ID</th>
                                        <th scope="col" class="text-center">Status </th>
                                        <th scope="col" class="text-center">Created</th>
                                        <th scope="col" class="text-center">Modified</th>
                                        <th scope="col" class="text-center">Action</th>
                                    </tr>
                                </thead>
                                @foreach (var row in Model)
                                {
                                    <tbody>
                                        <tr>
                                            <td class="text-center">@row.StatusID</td>
                                            <td class="text-center">@row.Status</td>
                                            <td class="text-center">@Convert.ToDateTime(row.Created).ToString("dd-MM-yyyy")</td>
                                            <td class="text-center">@Convert.ToDateTime(row.Created).ToString("dd-MM-yyyy")</td>
                                            <td>
                                                <div class="row">
                                                    <form method="post" class="form-group col-6" asp-area="Roomstatus" asp-controller="RoomStatus" asp-action="DeleteByStatusID" asp-route-StatusID="@row.StatusID">
                                                        <button type="submit" class="btn btn-danger btn-block">Delete</button>
                                                    </form>
                                                    <form class="form-group col-6 text-right" asp-area="Roomstatus" asp-controller="RoomStatus" asp-action="StatusAddEdit" asp-route-StatusID="@row.StatusID">
                                                        <button type="submit" class="btn btn-success btn-block">Edit</button>
                                                    </form>
                                                </div>
                                            </td>
                                        </tr>
                                    </tbody>
                                }
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div> *@
<div class="floating-container">
    <a asp-area="Roomstatus" asp-controller="RoomStatus" asp-action="StatusAddEdit"><div class="floating-button">+</div></a>
</div>
